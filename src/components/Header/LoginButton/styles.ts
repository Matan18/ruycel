import styled from "styled-components";
import { Button } from "../../Button";

export const LoginButtonContainer = styled(Button).attrs({
  colorType: 'purple'
})`
  width: 112px;
  height: 32px;
  border-radius: 5px;

  font-size: 16px;
  font-weight: 600;
  line-height: 19px;

`;
